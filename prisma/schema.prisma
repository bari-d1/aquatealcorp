generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Enrollment {
  id        Int      @id @default(autoincrement())
  firstName String
  lastName  String
  email     String
  phone     String?
  createdAt DateTime @default(now())
}
